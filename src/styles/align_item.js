import {StyleSheet, css} from "aphrodite";

export default StyleSheet.create({
    fs:{
        alignItems: "flex-start"
    },
    fe: {
        alignItems: "flex-end"
    },
    st:{
        alignItems: "stretch"
    },
    bl:{
        alignItems: "baseline"
    },
    c:{
        alignItems: "center"
    }
})
