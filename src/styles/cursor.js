/* .pointer{
    cursor: pointer;
} */

import {StyleSheet} from "aphrodite";

export default StyleSheet.create({
    pointer:{
        cursor: "pointer"
    },
    notAllow:{
        cursor: "not-allowed"
    }
})